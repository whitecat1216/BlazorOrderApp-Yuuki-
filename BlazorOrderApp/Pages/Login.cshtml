@* ============================================================
★ 重要 ★
ログイン／ログアウトはBlazorで書いてはいけない
Blazor全体をServerレンダーモードにしたいが、Cookie認証を利用する限り
ログインページはServerレンダーモードにできない
（認証はHTTP通信で行われる必要があり、SignalR通信中にはできないから）
そのため、ログインログアウトはBlazorから切り離し、razorで実現する。
============================================================ *@
@page "/login"
@using BlazorOrderApp.Pages
@using Microsoft.AspNetCore.Authorization
@attribute [AllowAnonymous]
@model LoginModel
@{
    Layout = "_EmptyLayout";
}

@if (Model.Expired)
{
    <div class="alert alert-warning">
        セッションの有効期限が切れました。再度ログインしてください。
    </div>
}

<div class="d-flex justify-content-center" style="margin-top: 120px;">
    <div class="card shadow-sm" style="width: 360px;">
        <div class="card-body">
            <h4 class="card-title text-center">受注管理デモ ログイン</h4>
            <div class="mb-4"></div>
            <form method="post" autocomplete="off">
                @Html.AntiForgeryToken()
                <div class="mb-3">
                    <label for="username" class="form-label">ユーザー名</label>
                    <!-- ? アイコンボタン -->
                    <button type="button" class="btn btn-link p-0 border-0 float-end"
                            data-bs-toggle="modal" data-bs-target="#testAccountModal"
                            title="テストアカウント情報">
                        <i class="bi bi-question-circle" style="font-size: 1.2rem;"></i>
                    </button>

                    <input id="username" name="UserName" class="form-control" value="@Model.UserName" autocomplete="username" />
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">パスワード</label>
                    <input id="password" name="Password" class="form-control" type="password" autocomplete="current-password" />
                </div>
   
                <button type="submit" class="btn btn-primary w-100" name="handler" value="Login">ログイン</button>

                <!-- デモログインボタン -->
                <div class="mt-4 d-grid gap-2">
                    <p>デモ専用簡易ボタン</p>

                    <button type="submit" class="btn btn-danger w-100" name="handler" value="DemoAdmin">管理者でログイン</button>
                    <button type="submit" class="btn btn-warning w-100" name="handler" value="DemoUser">一般でログイン</button>
                </div>

            </form>

            @if (Model.LoginError)
            {
                <div class="alert alert-danger mt-2">ログインに失敗しました。</div>
            }

        </div>
    </div>
</div>

<!-- 既存ログインカードのすぐ下に追加 -->
<div class="d-flex justify-content-center mt-3">
    <div class="card border-info shadow-sm" style="width: 360px;">
        <div class="card-body text-secondary" style="font-size: 0.9rem;">
            <ul class="mb-0 ps-3">
                <li>
                    データは自由に追加・修正・削除してください。<br />
                    （修正や削除は自分で追加したデータへお願いします）
                </li>
                <li>2025/10/29 受注明細の数量に少数以下が入力できていたバグを修正しました。</li>
            </ul>
        </div>
    </div>
</div>

<div class="modal fade" id="testAccountModal" tabindex="-1" aria-labelledby="testAccountModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="testAccountModalLabel">テストアカウント情報</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="閉じる"></button>
            </div>
            <div class="modal-body">
                <ul class="mb-0 ps-3">
                    <li><strong>管理者:</strong> admin / admin765</li>
                    <li><strong>一般ユーザー:</strong> test / test326</li>
                </ul>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">閉じる</button>
            </div>
        </div>
    </div>
</div>

<div class="developed">
    <span>Developed by</span><a href="https://hakamata-soft.com/" target="_blank">HakamataSoft</a>
</div>

<style>
    .developed {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        margin-top: 3rem;
        color: #666;
        font-size: 0.9rem;
    }

        .developed span {
            margin-right: 0.4rem;
        }

        .developed a {
            color: #3a78f2;
            text-decoration: none;
            font-weight: 600;
        }

            .developed a:hover {
                color: #77aaff;
                text-decoration: underline;
             }
</style>